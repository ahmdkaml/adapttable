/**
 * `@adapttable/core` — the headless engine behind AdaptTable.
 *
 * This package contains zero UI-kit, i18n-library, or router imports.
 * It exposes the {@link stableKey} helper today; the table state hooks,
 * the unified `TableSource` contract, and the `useDataTable` prop-getter
 * API land here as the core is built out.
 *
 * @packageDocumentation
 */

export { stableKey } from "./utils/stableKey";
