/**
 * English (`en`) label preset. This is exactly the core's built-in
 * English default set, re-exported here so the locale map is complete
 * without duplicating the strings.
 */
export { defaultLabels as en } from "@adapttable/core";
