---
"@adapttable/antd": patch
---

Group headers carry their `group-select` and `group-aggregate` part names, and
a group's footer no longer renders a second selection checkbox for rows its
header already controls.
